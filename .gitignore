*.idea
__pycache__/
# The `main` branch holds only code; generated source/output data live on the
# `derivatives` and `dist` branches, so keep them out of `main`.
sourcedata/*
derivatives/*
.DS_Store
